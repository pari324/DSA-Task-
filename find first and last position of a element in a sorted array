class Solution {
    public int[] searchRange(int[] nums, int target) {
        int[] arr={-1,-1};
        for(int i=0;i<nums.length;i++){
            if(target==nums[i]){
                arr[0]=i;
                break;
            }
        }
        for(int i=nums.length-1;i>=0;i--){
            if(target==nums[i]){
                arr[1]=i;
                break;
            }
        }
        return arr;
    }
}
